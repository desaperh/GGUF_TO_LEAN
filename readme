A formal verification tool that translates Lean theorem proofs into Rust code with constraint-based inverse analysis for GGUF quantized models.
Instead of solving: f⁻¹(output) = input
We compute: C(output) = {set of constraints on valid inputs}
